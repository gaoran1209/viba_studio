# 快速开始指南

## 🚀 5 分钟本地启动

### 1️⃣ 获取免费数据库（30 秒）

1. 访问 https://supabase.com
2. 点击 **Start your project**
3. 用 GitHub 登录
4. 填写：
   - **Name**: `viba-studio`
   - **Database Password**: 设置并记住密码！
   - **Region**: Singapore
5. 点击 **Create new project**
6. 等待 2 分钟...

### 2️⃣ 获取数据库连接（1 分钟）

项目创建完成后：
1. 点击左侧 **Settings** → **Database**
2. 找到 **Connection string**，选择 **URI** 格式
3. 复制字符串，格式类似：
   ```
   postgresql://postgres.xxxxx:YOUR_PASSWORD@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
   ```

### 3️⃣ 配置后端（30 秒）

打开 `backend/.env` 文件，替换这行：

```env
DATABASE_URL=你刚才复制的连接字符串
```

### 4️⃣ 安装依赖并启动（2 分钟）

```bash
# 安装后端依赖
cd backend
npm install

# 启动后端（保持这个终端运行）
npm run dev
```

**新开一个终端**：

```bash
# 安装前端依赖
npm install

# 启动前端
npm run dev
```

### 5️⃣ 测试应用

1. 打开浏览器访问：http://localhost:3000
2. 点击 **Sign up** 注册账户
3. 登录后进入 **Settings** 设置 Gemini API Key
4. 开始使用！

---

## ❓ 遇到问题？

### 问题 1: "Cannot find module"
**解决**: 删除 `node_modules` 和 `package-lock.json`，重新 `npm install`

### 问题 2: "Database connection failed"
**解决**: 检查 `backend/.env` 中的 `DATABASE_URL` 是否正确

### 问题 3: 页面空白
**解决**: 确保后端和前端都在运行，检查浏览器控制台错误

---

## 📱 下一步：云端部署

查看 [DEPLOYMENT.md](./DEPLOYMENT.md) 了解如何免费部署到云端。
